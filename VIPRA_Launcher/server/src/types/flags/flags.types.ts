export enum FLAGS {
  FLAGS_FILE          = '-flagsfile',
  REBUILD             = '-rebuild',
  DEBUG_SETUP         = '-debugsetup',
  DEBUG_BUILD         = '-debugbuild',
  NO_SERV   = '-noserver',
  NO_BUILD_CLEAN = '-noclean',
}