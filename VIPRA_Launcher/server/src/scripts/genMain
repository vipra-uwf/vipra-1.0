while [ $# -gt 0 ]; do
    if [[ $1 == "--"* ]]; then
        v="${1/--/}"
        declare "$v"="$2"
        shift
    fi
    shift
done

if [ -z ${vipraDir+x} ]
then
    echo "[ERROR] No vipraDir was provided"
    exit -1
fi

make -C ${vipraDir}