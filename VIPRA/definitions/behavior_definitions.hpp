#ifndef BEHAVIOR_DEFINITIONS
#define BEHAVIOR_DEFINITIONS

/**
 * Definitions for a hard-coded human behavior model. Eventually this is also defined in the DSL.
 */
enum struct BehaviorDefinitions
{
    AWAKE, SLEEPING
};

#endif // BEHAVIOR_DEFINITIONS